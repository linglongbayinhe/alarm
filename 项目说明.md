# 项目说明

## 项目目标
- 基于 `ESP32-WROOM-32E` 实现智能闹钟底座。
- 手机通过 BLUFI 下发 Wi-Fi 配网信息；ESP32 联网后启动 SNTP 校时。
- 无网或 SNTP 未同步时，优先使用 `DS3231` RTC 恢复本地时间。
- 通过 `ST7789` SPI 彩屏显示联网状态、日期、时间和天气信息。
- 从云端拉取闹钟任务，按 `ringAt` 本地播放默认音频或缓存音频。

## 当前核心结构
- `main/main.c` 是主入口，负责初始化 NVS、云端配置、SPIFFS、音频、显示、时间、RTC、BLUFI/Wi-Fi，以及运行时服务启动。
- `main/device_cloud_service.c` 管理云端默认 URL、`clientId/deviceId`、HTTP session 基础能力和请求 body 生成。
- `main/network_task_service.c` 是统一网络执行器，所有 HTTPS/TLS 请求都从这里串行执行，避免多个 task 同时握手导致 heap/连续内存不足。
- `main/playback_task_service.c` 管理闹钟任务同步、本地任务持久化、播放调度、`targetVolume` 音量更新和播放后补拉取。
- `main/weather/weather_device_service_provider.c` 管理天气刷新节奏和天气 JSON 解析，但不直接发 HTTPS。
- `main/audio/audio_service.c` 负责 WAV/MP3 播放、音量缩放和 I2S 输出。
- `main/audio/audio_cache_service.c` 负责云端音频缓存路径、文件存在检查和下载执行；下载由统一网络队列触发。
- `main/storage_service.c` 挂载 SPIFFS，并提供默认音频路径。

## 运行时 FreeRTOS 任务
- `main_task`：ESP-IDF 启动后调用 `app_main()`，完成 NVS、云端配置、存储、音频、屏幕、时间、RTC、BLUFI/Wi-Fi 等初始化；初始化完成后返回。
- `wifi driver task`：ESP-IDF Wi-Fi 驱动内部任务，负责 Wi-Fi 连接、断线重连、收发包和 IP 事件。
- `BLUFI task`：蓝牙配网相关任务，负责接收手机下发的 Wi-Fi 和 custom data；Wi-Fi 获取 IP 后释放 BLE/BLUFI 资源。
- `network_task`：项目统一网络执行器，串行执行 `pullPlaybackTasks`、`audio_cache_maintenance`、`playback_report`、`getCurrentWeather` 等网络任务。
- `playback_task`：闹钟调度任务，负责定时拉取闹钟、维护本地闹钟队列、到点播放、播放后补拉取。
- `weather_device task`：天气刷新节奏任务，默认联网后延迟 20 秒提交首次天气刷新，之后按 15 分钟节奏提交 `weather_refresh`。
- `LVGL/display task`：屏幕 UI 任务，由 LVGL/esp_lvgl_port 驱动，负责界面刷新、时间/天气/联网状态显示。

## 云端接口
- 默认天气接口：`https://alarm.yoyospace.cc/device-service/getCurrentWeather`
- 默认闹钟拉取接口：`https://alarm.yoyospace.cc/device-service/pullPlaybackTasks`
- 默认状态上报接口：`https://alarm.yoyospace.cc/device-service/reportPlaybackTaskStatus`
- 固件运行时始终使用 `DEVICE_CLOUD_DEFAULT_*_URL` 默认 URL。
- URL 不再持久化到 NVS；NVS 中只保留 `clientId/deviceId/auth/轮询间隔` 等必要配置。
- 重新烧录 app 后会直接使用固件内默认 URL，不需要清 NVS 才能切换 URL。
- BLUFI custom data 仍可更新 `clientId/deviceId` 等配置；URL 字段即使下发，也会被运行时默认 URL 覆盖。

### `getCurrentWeather` 返回示例
请求日志示例：

```text
POST https://alarm.yoyospace.cc/device-service/getCurrentWeather
```

成功响应示例：

```json
{
  "success": true,
  "deviceId": "dev_demo_001",
  "city": "北京北京朝阳区越景路",
  "weatherText": "晴",
  "temperature": 25,
  "weatherIcon": "sunny",
  "weatherUpdatedAt": "2026-04-29T09:41:23.412Z"
}
```

固件处理逻辑：解析 `city/weatherText/temperature/weatherIcon/weatherUpdatedAt` 并更新天气显示；天气接口不处理音量字段。

### `pullPlaybackTasks` 返回示例
请求日志示例：

```text
POST https://alarm.yoyospace.cc/device-service/pullPlaybackTasks
```

成功响应示例：

```json
{
  "targetVolume": 40,
  "currentVolume": 60,
  "volumeLevel": 5,
  "tasks": [
    {
      "instanceId": "instance_alarm_1776770757934_20260421233000000",
      "alarmId": "alarm_1776770757934",
      "deviceId": "dev_demo_001",
      "ringAt": "2026-04-21T23:30:00.000Z",
      "title": "起床",
      "voice": "温柔派",
      "nickname": "人哥哥",
      "status": "pending",
      "scriptStatus": "generated",
      "audioStatus": "generated",
      "audioUrl": "https://mp-569ac274-a245-482f-994d-e065e5e73b0b.cdn.bspapp.com/cloudstorage/9782c899-9824-4fdf-97a5-7bbbf12d537d.mp3",
      "fallbackMode": "device_local_realtime_mix"
    },
    {
      "instanceId": "instance_alarm_1776770757934_20260421233000000",
      "alarmId": "alarm_1776770757934",
      "deviceId": "dev_demo_001",
      "ringAt": "2026-04-29T17:41:00.000Z",
      "title": "1",
      "voice": "温柔派",
      "nickname": "人哥哥",
      "status": "pending",
      "scriptStatus": "generated",
      "audioStatus": "generated",
      "audioUrl": "https://mp-569ac274-a245-482f-994d-e065e5e73b0b.cdn.bspapp.com/cloudstorage/9782c899-9824-4fdf-97a5-7bbbf12d537d.mp3",
      "fallbackMode": "device_local_realtime_mix"
    }
  ]
}
```

固件处理逻辑：顶层 `targetVolume` 会设置当前闹钟音量；过去时间的 `ringAt` 不保存、不播放；有效未来闹钟进入本地队列，并触发一次 `audio_cache_maintenance` 检查/下载缺失的 `audioUrl`。

## 统一网络队列
- 所有云端/TLS 请求统一进入 `network_task_service`，由一个 worker task 串行执行。
- 网络任务优先级为：`post_playback_pull` > `playback_pull` > `audio_cache_maintenance` > `playback_report` > `weather_refresh`。
- `post_playback_pull`：闹钟播放结束后触发的一次高优先级 `pullPlaybackTasks`。
- `playback_pull`：普通闹钟拉取，包括开机首次拉取、定时拉取和手动同步。
- `audio_cache_maintenance`：维护 W25Q128 外部音频缓存；按有效闹钟列表检查 `audioUrl` 是否已有缓存，缺失时下载，空间不足时先清理无关旧音频。
- `playback_report`：上报闹钟状态，例如缓存成功后的 `READY`；`finished/failed` 当前不上报。
- `weather_refresh`：拉取 `getCurrentWeather`，默认 15 分钟一次。
- 同类任务去重：闹钟拉取和天气刷新队列中各只保留一条；音频缓存维护同一时间只保留一条最新快照；状态上报按 `instanceId + status` 去重。
- 每次 HTTPS 失败后会清理 HTTP session，下次重新创建，避免复用坏连接。

## 播放后补拉取与 heap 策略
- 播放闹钟前会请求统一网络服务释放当前 HTTP/TLS session。
- 播放结束后不再固定等待 60 秒，而是提交 `post_playback_pull`。
- `post_playback_pull` 执行前检查 heap：`free >= 50000` 且 `largest >= 28000`。
- heap 条件需要连续 2 次满足，检查间隔 2 秒。
- 如果 90 秒内仍不满足，会打印 heap 日志并尝试执行一次，避免永久不拉取。
- `free` 表示总可用动态内存；`largest` 表示最大连续可申请内存。TLS 握手更依赖 `largest`。

## 闹钟与音量
- `pullPlaybackTasks` 返回的顶层 `targetVolume` 会更新当前闹钟播放音量。
- 如果顶层没有 `targetVolume`，会尝试读取任务数组中第一个有效 `targetVolume`。
- 音量范围按 `0..100` 夹取；缺失时保留当前音量。
- 首次默认音量为 `50%`。
- `targetVolume=0` 表示静音；大于 `100` 按 `100` 处理。
- 本地默认音频和云端缓存音频都使用同一当前音量。

## 音频资源
- 项目自有音频统一放在仓库根目录 `audio/`。
- SPIFFS 打包源目录为 `audio/`。
- 当前默认音频路径为 `/spiffs/default_alarm.wav`。
- 第三方组件测试音频不打包进项目。
- 默认音频可直接从 SPIFFS 播放；云端 `audioUrl` 只有在外部缓存可用时才下载缓存。
- `audioUrl` 缓存文件名按 `audioUrl hash + 扩展名` 生成，同一个 URL 多个闹钟只保存一份。
- 下载状态可通过 monitor 日志判断：
  - `NETWORK_TASK: Queued audio_cache_maintenance items=N`：闹钟拉取后已提交缓存维护任务。
  - `NETWORK_TASK: Running audio_cache_maintenance items=N`：开始检查/下载本轮音频。
  - `AUDIO_CACHE: Cache hit: path=... url=...`：W25Q128 已有该 `audioUrl` 缓存，不会重复下载。
  - `AUDIO_CACHE: Download start: url=... path=...`：开始下载该 `audioUrl` 到 `.part` 文件。
  - `AUDIO_CACHE: Download headers: status=... content_length=... path=...`：已拿到 HTTP 响应头和文件大小。
  - `AUDIO_CACHE: Download finished: path=... bytes=... url=...`：下载完成并重命名为正式缓存文件。
  - `AUDIO_CACHE: Download failed: path=... ret=... url=...`：下载失败，`.part` 会被删除。
  - `PLAYBACK_TASK: Audio cache result: status=cached ...`：闹钟服务已把该 `audioUrl` 标记为最近一次缓存成功。
  - `PLAYBACK_TASK: Audio cache result: status=waiting ...`：空间不足或文件被清理，保持等待；到点时会 fallback 默认音频。
  - `PLAYBACK_TASK: Audio cache result: status=failed ...`：下载或缓存处理失败；到点时会 fallback 默认音频。

## 天气显示
- 天气服务调用 `getCurrentWeather`，请求 body 固定包含 `{ clientId, deviceId }`。
- 成功响应字段包括 `success/deviceId/city/weatherText/temperature/weatherIcon/weatherUpdatedAt`。
- 天气服务不再处理音量字段；闹钟音量只来自 `pullPlaybackTasks.targetVolume`。
- 天气刷新默认每 15 分钟一次，首次联网后延迟 20 秒提交刷新任务。

## 本地持久化
- 云端配置 NVS namespace：`dev_cloud`，key：`config_v1`。
- 闹钟任务 NVS namespace：`playback`，key：`tasks_v1`。
- 已播放或失败的任务会在本地标记，避免同一次闹钟重复播放。
- 播放结果 `finished/failed` 当前只打印本地日志，不主动上报云端。

## 构建与验证要点
- `idf.py build` 应能从 `audio/` 生成 SPIFFS 镜像。
- 启动日志应显示实际云端 URL：
  - `Cloud URLs: weather=... pull=... report=...`
- 网络请求日志应由 `NETWORK_TASK` 统一发起。
- 播放结束后应看到：
  - `post_playback_pull pending`
  - heap 满足后执行 `pullPlaybackTasks`
- 不应再出现天气 task 和闹钟 task 同时进入 TLS 握手。
- 如果仍出现 TLS 内存错误，优先查看日志中的 `free/largest`，再调整 heap 阈值或释放更多音频/I2S 资源。
